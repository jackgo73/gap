#include "gtest/gtest.h"
#include "p_00437_path_sum.c"
#include "ut_00000.h"

TEST(PATH_SUM, TEST01) {
    struct TreeNode *root = (struct TreeNode *) malloc(sizeof(struct TreeNode) * 9);
    root[0].val = 10;


    EXPECT_EQ (1, 1);
}
